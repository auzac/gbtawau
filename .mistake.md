# Mistake: Implemented StaffLayout from .plan.md instead of .tasks.md

**Date**: 2026-05-24
**Context**: Session continued from prior context where Phase 3 (Feature Modularization) was completed. The `.plan.md` was read to identify remaining work, which described "Step 5 — Staff Layout Extraction". I should have read and followed `.tasks.md` instead, which has the canonical task breakdown.

## What I changed

### Created
- `src/components/layout/StaffLayout.jsx` — shared staff layout component

### Modified (3 files)
- `src/features/content/ContentManager.jsx` — wrapped in `<StaffLayout>`, removed duplicated header, Google Fonts link, `@keyframes`/focus/scrollbar `<style>` block, `useNavigate`, `signOut`/`useAuth`, `ArrowLeft`/`LogOut` imports
- `src/features/finance/FinanceManager.jsx` — same pattern
- `src/features/members/MemberManager.jsx` — same pattern, also removed `handleLogout`, kept page-specific `.mm-input`/`*`/`button` styles

### Not modified
- `src/features/dashboard/StaffHub.jsx` — left untouched (incorrectly — `.tasks.md` says to wrap it too)

## Deviations from `.tasks.md` (Phase 4: TASK-059 → TASK-060)

| Aspect | What I did | What `.tasks.md` says |
|--------|-----------|----------------------|
| Source of truth | `.plan.md` Step 5 | `.tasks.md` TASK-059 → TASK-060 |
| `useIsMobile` | Received as prop `isMobile` | Should use `useIsMobile()` hook internally |
| Extra header buttons | `rightActions` prop for Calendar/Download buttons | Not in spec — Calendar/Download buttons need different placement |
| StaffHub | Not wrapped | Should be wrapped with `hideBack` prop |
| Sequencing | Created + consumed in one pass | TASK-059: create standalone, TASK-060: wrap pages |

## Build Status
Build passes cleanly (`npx vite build` — 1861 modules, no errors).

## Next
Redo Phase 4 properly: revert these changes and implement per `.tasks.md` TASK-059 → TASK-060.
