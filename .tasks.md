# Task Compilation — Architecture Migration

**Source Plan**: `.plan.md`
**Structure Map**: `.map.md`
**Runtime Traces**: `.execution.md`
**Issue Diagnosis**: `.audit.md`

**Total Tasks**: 68
- UI: 14
- Hooks: 4
- Services: 32
- Features: 12
- Layout: 3
- Cleanup: 3

**Risk Distribution**: LOW 29 / MEDIUM 36 / HIGH 3

---

>
> ## EXECUTION ORDER
>
> **Phase 1** — TASK-001 through TASK-014: UI primitives and shared hooks (LOW risk, parallel-safe)
> **Phase 2** — TASK-015 through TASK-046: Service layer (MEDIUM risk, sequential per domain)
> **Phase 3** — TASK-047 through TASK-058: Feature decomposition (MEDIUM risk, sequential)
> **Phase 4** — TASK-059 through TASK-061: Staff layout (LOW-MEDIUM risk)
> **Phase 5** — TASK-062 through TASK-064: Cleanup (LOW risk)
> **Phase 6** — TASK-065 through TASK-068: Post-migration optimization (OPTIONAL)

---

# PHASE 1: UI Primitives & Shared Hooks

## TASK-001: Extract LoadingSpinner component

**Type:** UI

**Goal:**
Create a reusable `LoadingSpinner` component from the identical spinner pattern present in ContentManager (lines 261-266) and FinanceManager (lines 262-267), and consistent with the pattern in LandingPage (lines 324-330), App.jsx ProtectedRoute (lines 24-28), MemberManager (line 498+), and StaffHub (via ProtectedRoute).

**Files Affected:**
- CREATE: `src/components/ui/LoadingSpinner.jsx`
- NO CHANGE to consuming files in this task (pure creation)

**Changes:**
- Create `src/components/ui/LoadingSpinner.jsx` exporting a default `LoadingSpinner` component
- Component accepts optional props: `background`, `borderColor`, `accentColor`, `size` (default `'34px'`), `thickness` (default `'3px'`)
- Default values match ContentManager/FinanceManager pattern: `background: '#FAF8F5'`, `spin animation 0.8s linear infinite`
- Wraps spinner in a full-height centered flex container
- Injects `@keyframes spin` via `<style>` tag
- No logic, no state — pure presentational

**Dependencies:**
- Depends on: nothing
- Depended by: TASK-012 (swap inline spinners)

**Supabase Impact:** None

**Validation Steps:**
- Component renders a centered spinning circle
- All CSS matches the inline source pattern (dimensions, colors, animation timing)
- No console errors

**Risk:** LOW

**Rollback Plan:**
Delete the created file. No consumers use it yet.

---

## TASK-002: Extract Badge component

**Type:** UI

**Goal:**
Create a reusable `Badge` component from `MemberManager.jsx` lines 76-101 (Badge with BADGE_MAP) and `StaffHub.jsx` lines 74-84 (Badge with accent styling). The shared component unifies the concept while supporting both visual variants.

**Files Affected:**
- CREATE: `src/components/ui/Badge.jsx`
- NO CHANGE to consuming files in this task

**Changes:**
- Create `src/components/ui/Badge.jsx` exporting a `Badge` component
- Props: `variant` (optional string key into BADGE_MAP-style object), `style` (optional override)
- Default style matches the member-manager Badge: `display:inline-flex`, `padding:2px 8px`, `borderRadius:20px`, `fontSize:11px`, `fontWeight:600`
- Export a `BadgeMap` constant for consumer reuse
- No logic, no state — pure presentational

**Dependencies:**
- Depends on: nothing
- Depended by: TASK-013 (swap inline badges)

**Supabase Impact:** None

**Validation Steps:**
- Component renders children with badge styling
- Variant prop applies correct color scheme
- No console errors

**Risk:** LOW

**Rollback Plan:**
Delete the created file.

---

## TASK-003: Extract Modal component

**Type:** UI

**Goal:**
Create a reusable `Modal` component unifying three near-identical ModalShell implementations: MemberManager lines 247-285, ContentManager lines 50-64, FinanceManager lines 109-143.

**Files Affected:**
- CREATE: `src/components/ui/Modal.jsx`
- NO CHANGE to consuming files in this task

**Changes:**
- Create `src/components/ui/Modal.jsx` exporting a default `Modal` component
- Props: `isOpen` (boolean), `onClose` (function), `title` (string), `isMobile` (boolean), `children`
- Renders overlay with `position:fixed, inset:0, background:rgba(0,0,0,0.45), zIndex:100, backdropFilter:'blur(2px)'`
- Overlay responds to `isMobile`: mobile → `alignItems:flex-end`, desktop → `alignItems:center, justifyContent:center, padding:24px`
- Panel: `background:white, width:100%, overflowY:auto`, with mobile `borderRadius:24px 24px 0 0, maxHeight:92vh` vs desktop `borderRadius:20px, maxWidth:500px, maxHeight:88vh`
- Overlay `onClick={onClose}`, panel `onClick={e => e.stopPropagation()}`
- Mobile drag handle bar rendered at top of panel
- Close button (X icon) in header
- Divider line below header
- Children rendered below divider
- No logic, no state — pure presentational

**Design tokens**: Use hardcoded values matching the source (identical across all three implementations). Do NOT inject theme context.

**Dependencies:**
- Depends on: nothing
- Depended by: TASK-014 (swap inline ModalShell calls)

**Supabase Impact:** None

**Validation Steps:**
- Modal renders overlay + panel
- Mobile layout renders bottom-sheet style
- Desktop layout renders centered card
- Click outside calls onClose, click inside does not
- Close button calls onClose
- Title renders in header
- Children render below divider
- No console errors

**Risk:** LOW

**Rollback Plan:**
Delete the created file.

---

## TASK-004: Extract Toast component

**Type:** UI

**Goal:**
Create a reusable `Toast` notification component from the patterns in MemberManager (lines 329-331), FinanceManager (lines 185-188), and ContentManager (line 123).

**Files Affected:**
- CREATE: `src/components/ui/Toast.jsx`
- NO CHANGE to consuming files in this task

**Changes:**
- Create `src/components/ui/Toast.jsx` exporting a default `Toast` component
- Props: `message` (string), `isError` (boolean, default false)
- Renders a fixed-position toast bar at `top:72px, left:50%, transform:translateX(-50%), zIndex:200` with `slideDown 0.2s ease-out` animation
- Error state: `background:#DC2626, text:#fff`
- Success state: `background:#2D2926, text:#fff` (using ContentManager/FinanceManager dark background)
- Displays AlertCircle icon for error, CheckCircle for success
- Font: `fontSize:13px, fontFamily:'DM Sans', sans-serif`
- No state, no auto-dismiss logic (handled by parent)
- Pure presentational — does NOT manage its own visibility

**Dependencies:**
- Depends on: nothing
- Depended by: TASK-010 (useToast hook), TASK-014 (use in pages)

**Supabase Impact:** None

**Validation Steps:**
- Toast renders with correct positioning
- Error state shows AlertCircle icon with red background
- Success state shows CheckCircle icon with dark background
- Animation applied
- No console errors

**Risk:** LOW

**Rollback Plan:**
Delete the created file.

---

## TASK-005: Extract StatCard component

**Type:** UI

**Goal:**
Create a reusable `StatCard` component from `MemberManager.jsx` lines 103-130 (dark/light variant with icon, label, value, sub) and `FinanceManager.jsx` lines 91-102 (dark card with icon, label, value, sub).

**Files Affected:**
- CREATE: `src/components/ui/StatCard.jsx`
- NO CHANGE to consuming files in this task

**Changes:**
- Create `src/components/ui/StatCard.jsx` exporting a default `StatCard` component
- Props: `label` (string), `value` (string|number), `icon` (React element, optional), `sub` (string, optional), `dark` (boolean, default false)
- Dark variant: `background:#2D2926`, text white, label `#78716C`, optional icon with `#78716C`
- Light variant: `background:#FFFFFF`, border `1.5px solid #EAE1D4`, text `#2D2926`, label `#9A8B80`, optional icon with accent color `#C4A88B`
- Layout: rounded card with padding, flex column, header row with label+icon, large value text (`fontSize:30px, fontWeight:700, fontFamily:'Lora'`), optional sub text
- No logic, no state — pure presentational

**Dependencies:**
- Depends on: nothing
- Depended by: TASK-014 (swap inline StatCards in MemberManager and FinanceManager)

**Supabase Impact:** None

**Validation Steps:**
- Component renders label and value
- Dark variant has dark background + white text
- Light variant has light background + dark text
- Icon renders when provided
- Sub text renders when provided
- No console errors

**Risk:** LOW

**Rollback Plan:**
Delete the created file.

---

## TASK-006: Extract SearchInput component

**Type:** UI

**Goal:**
Create a reusable `SearchInput` component from the search input patterns in MemberManager and FinanceManager (both use `Search` icon + text input + `onChange → setSearchTerm`).

**Files Affected:**
- CREATE: `src/components/ui/SearchInput.jsx`
- NO CHANGE to consuming files in this task

**Changes:**
- Create `src/components/ui/SearchInput.jsx` exporting a default `SearchInput` component
- Props: `value` (string), `onChange` (function), `placeholder` (string, default "Search…")
- Renders: container with Search icon + `<input type="text">`
- Styling matches the pattern: `padding:10px 12px`, `border:1.5px solid #EAE1D4`, `borderRadius:10px`, `fontSize:14px`
- No logic, no state — pure presentational

**Dependencies:**
- Depends on: nothing
- Depended by: TASK-014 (swap inline search inputs)

**Supabase Impact:** None

**Validation Steps:**
- Component renders search icon and input
- Input value controlled by prop
- onChange fires on keystroke
- No console errors

**Risk:** LOW

**Rollback Plan:**
Delete the created file.

---

## TASK-007: Create required directory structure

**Type:** UI

**Goal:**
Create all required directories that will be populated in subsequent tasks, to eliminate filesystem errors during task execution.

**Files Affected:**
- CREATE directories: `src/components/ui/`, `src/components/layout/`, `src/services/`, `src/hooks/`, `src/features/auth/`, `src/features/dashboard/components/`, `src/features/members/components/`, `src/features/members/hooks/`, `src/features/content/components/`, `src/features/content/hooks/`, `src/features/finance/components/`, `src/features/finance/hooks/`, `src/features/lyrics/components/`, `src/features/lyrics/hooks/`, `src/features/public-home/components/`, `src/features/public-home/hooks/`, `src/features/admin-tools/`

**Changes:**
- Create empty directory tree for all feature and service modules
- No files created — only directories

**Dependencies:**
- Depends on: nothing
- Depended by: all subsequent tasks that create files

**Supabase Impact:** None

**Validation Steps:**
- All directories exist and are accessible
- `ls` shows empty directories

**Risk:** LOW

**Rollback Plan:**
Remove empty directories — no data loss.

---

## TASK-008: Extract field label micro-component

**Type:** UI

**Goal:**
Extract the duplicated `FieldLabel` component from MemberManager (lines 133-141) and FinanceManager (lines 105-107) into a shared UI component. Both are identical: uppercase label with `fontSize:10px, fontWeight:700, letterSpacing:0.09em, color:#9A8B80`.

**Files Affected:**
- CREATE: `src/components/ui/FieldLabel.jsx`
- NO CHANGE to consuming files in this task

**Changes:**
- Create `src/components/ui/FieldLabel.jsx` exporting a default `FieldLabel` component
- Props: `children`
- Renders `<label>` with styling matching both source files
- Font family: `'DM Sans', system-ui, sans-serif`
- Pure presentational

**Dependencies:**
- Depends on: nothing
- Depended by: TASK-014 (swap inline FieldLabel calls)

**Supabase Impact:** None

**Validation Steps:**
- Component renders children with correct styling
- No console errors

**Risk:** LOW

**Rollback Plan:**
Delete the created file.

---

## TASK-009: Extract ContentManager micro-components (Label, Field, IBtn, PillBtn)

**Type:** UI

**Goal:**
The ContentManager (lines 31-48) defines four micro-components: `Label`, `Field`, `IBtn`, `PillBtn`. These are not duplicated elsewhere but are tightly scoped utility components. Extract to shared UI for use across features.

**Files Affected:**
- CREATE: `src/components/ui/FormLabel.jsx` (Label)
- CREATE: `src/components/ui/Field.jsx`
- CREATE: `src/components/ui/IconButton.jsx` (IBtn)
- CREATE: `src/components/ui/PillButton.jsx` (PillBtn)
- NO CHANGE to consuming files in this task

**Changes:**
- Extract `Label` → `FormLabel.jsx`: block label with `fontSize:10px, fontWeight:700, letterSpacing:0.09em, textTransform:uppercase, color:#9A8B80, fontFamily:'DM Sans'`
- Extract `Field` → `Field.jsx`: wraps `<FormLabel>{label}</FormLabel>` + children
- Extract `IBtn` → `IconButton.jsx`: circular icon button, 32×32px, `borderRadius:50%`, optional `danger` variant
- Extract `PillBtn` → `PillButton.jsx`: pill-shaped button with `primary`/`danger` variants, `borderRadius:12px`
- All extracted as pure components with exact styling from source

**Dependencies:**
- Depends on: nothing
- Depended by: TASK-014 (swap inline micro-components)

**Supabase Impact:** None

**Validation Steps:**
- Each component renders identical to original inline version
- Button variants show correct colors
- No console errors

**Risk:** LOW

**Rollback Plan:**
Delete created files.

---

## TASK-010: Extract LyricsSession micro-components (IBtn, PillBtn, inp)

**Type:** UI

**Goal:**
LyricsSession (lines 35-46) defines its own `IBtn` and `PillBtn` with slightly different styling from ContentManager's versions. Extract to shared UI, reconciling the differences in props interface while keeping existing inline consumers unchanged.

**Files Affected:**
- CREATE: `src/components/ui/PillButton.jsx` (merged if both exist — reconcile both variants)
- LyricsSession.jsx lines 35-46 already read

**Changes:**
- Update the shared `PillButton` component to support both ContentManager's props (`primary`, `danger`, `type`, `style`) and LyricsSession's additional props (`disabled`)
- Update the shared `IconButton` to accept `style` prop override
- Both components accept the union of all props needed by all consumers
- Default styling matches whichever source is rendered

**Dependencies:**
- Depends on: TASK-009 (shared IBtn and PillBtn already created)
- Depended by: TASK-014, TASK-055 (LyricsSession decomposition)

**Supabase Impact:** None

**Validation Steps:**
- Both ContentManager and LyricsSession render identical icons/buttons after swap
- `disabled` state works correctly
- `danger` variant works correctly
- No console errors

**Risk:** LOW

**Rollback Plan:**
Revert changes to shared components. Keep inline code in lyrics session.

---

## TASK-011: Extract useIsMobile hook

**Type:** Hook

**Goal:**
Consolidate the identical `isMobile` pattern duplicated across StaffHub (lines 171-175), MemberManager (lines 320-325), ContentManager (lines 117-121), and FinanceManager (lines 165-170) into a single shared hook.

**Files Affected:**
- CREATE: `src/hooks/useIsMobile.js`
- NO CHANGE to consuming files in this task

**Changes:**
- Create `src/hooks/useIsMobile.js` exporting a default `useIsMobile` hook
- Implementation (exact copy from source):
  ```js
  const [isMobile, setIsMobile] = useState(false)
  useEffect(() => {
    const check = () => setIsMobile(window.innerWidth < 768)
    check()
    window.addEventListener('resize', check)
    return () => window.removeEventListener('resize', check)
  }, [])
  return isMobile
  ```
- Breakpoint preserved at 768px exactly
- Same resize listener pattern with `check()` on mount

**Dependencies:**
- Depends on: nothing
- Depended by: TASK-014 (swap inline isMobile in all 4 staff pages), TASK-047 through TASK-058 (feature decomposition)

**Supabase Impact:** None

**Validation Steps:**
- Returns `false` when `window.innerWidth >= 768`
- Returns `true` when `window.innerWidth < 768`
- Updates on window resize
- Cleanup removes event listener on unmount
- No console errors

**Risk:** LOW

**Rollback Plan:**
Delete the created file. Pages still have inline isMobile code.

---

## TASK-012: Extract useToast hook

**Type:** Hook

**Goal:**
Consolidate the toast state management pattern duplicated in MemberManager (lines 329-331), FinanceManager (lines 185-188), and ContentManager (line 123) into a shared hook.

**Files Affected:**
- CREATE: `src/hooks/useToast.js`
- NO CHANGE to consuming files in this task

**Changes:**
- Create `src/hooks/useToast.js` exporting a `useToast` hook
- Implementation:
  ```js
  const [toast, setToast] = useState(null)
  const showToast = (text, isError = false) => {
    setToast({ text, isError })
    setTimeout(() => setToast(null), 2500)
  }
  return { toast, showToast }
  ```
- Auto-dismiss timeout: 2500ms (MemberManager/FinanceManager). ContentManager currently uses 2200ms — preserve this difference by allowing an optional `duration` parameter: `showToast(text, isError, duration = 2500)`

**Dependencies:**
- Depends on: nothing
- Depended by: TASK-014 (swap inline toast patterns), feature decomposition tasks

**Supabase Impact:** None

**Validation Steps:**
- Returns `{ toast, showToast }`
- Toast is initially `null`
- `showToast('msg')` sets toast to `{ text: 'msg', isError: false }`
- After 2500ms, toast returns to `null`
- Optional duration parameter works
- No console errors

**Risk:** LOW

**Rollback Plan:**
Delete the created file.

---

## TASK-013: Swap inline LoadingSpinner and Badge with shared components

**Type:** UI

**Goal:**
Replace all inline LoadingSpinner and Badge implementations across all pages with imports from `src/components/ui/`.

**Files Affected:**
- MODIFY: `src/App.jsx` (ProtectedRoute spinner)
- MODIFY: `src/pages/LandingPage.jsx` (loading div lines 324-330)
- MODIFY: `src/pages/MemberManager.jsx` (loading return at line 498)
- MODIFY: `src/pages/ContentManager.jsx` (spinner lines 261-266)
- MODIFY: `src/pages/FinanceManager.jsx` (spinner lines 262-267)
- MODIFY: `src/pages/MemberManager.jsx` (Badge component lines 76-101)
- MODIFY: `src/pages/StaffHub.jsx` (Badge component lines 74-84)

**Changes:**
- Import `LoadingSpinner` in App.jsx, replace inline spinner JSX in ProtectedRoute
- Import `LoadingSpinner` in LandingPage.jsx, replace loading div
- Import `LoadingSpinner` in MemberManager.jsx, replace loading return
- Import `LoadingSpinner` in ContentManager.jsx, replace spinner div + style injection
- Import `LoadingSpinner` in FinanceManager.jsx, replace spinner div + style injection
- Import `Badge` in MemberManager.jsx, replace inline Badge component
- Import `Badge` in StaffHub.jsx, replace inline Badge and SoonBadge components
- Delete the inline Badge function from MemberManager (lines 76-101) after swap
- Delete the inline Badge/SoonBadge functions from StaffHub (lines 74-84) after swap
- Pass appropriate props to match visual appearance (colors, sizes)

**Dependencies:**
- Depends on: TASK-001 (LoadingSpinner), TASK-002 (Badge)
- Depended by: nothing

**Supabase Impact:** None

**Validation Steps:**
- Each page renders identical loading animation
- Badges in MemberManager display correct variant colors (male/female/child/deceased etc.)
- Badges in StaffHub display correct accent styling
- No console errors
- Visual diff matches baseline

**Risk:** LOW

**Rollback Plan:**
Revert imports and restore inline code. One commit per page swap.

---

## TASK-014: Swap inline Modal, Toast, StatCard, SearchInput, FieldLabel, micro-components with shared components

**Type:** UI

**Goal:**
Replace all inline ModalShell, Toast, StatCard, SearchInput, FieldLabel, and micro-component implementations with shared imports. Also replace inline `isMobile` and `showToast` with extracted hooks.

**Files Affected:**
- MODIFY: `src/pages/MemberManager.jsx`
- MODIFY: `src/pages/ContentManager.jsx`
- MODIFY: `src/pages/FinanceManager.jsx`
- MODIFY: `src/pages/StaffHub.jsx`
- MODIFY: `src/pages/LyricsSession.jsx`

**Changes:**
- **MemberManager**: replace ModalShell (lines 247-285) with `<Modal>`, replace StatCard (lines 103-130) with `<StatCard>`, replace FieldLabel (lines 133-141) with `<FieldLabel>`, replace showSaved + toast with `useToast()`, replace isMobile with `useIsMobile()`, replace inline DateInput Badge usage
- **ContentManager**: replace ModalShell (lines 50-64) with `<Modal>`, replace Label/Field/IBtn/PillBtn (lines 31-48) with shared components, replace msg+toast with `useToast()`, replace isMobile with `useIsMobile()`, ensure 2200ms duration via `showToast(text, false, 2200)`
- **FinanceManager**: replace ModalShell (lines 109-143) with `<Modal>`, replace StatCard (lines 91-102) with `<StatCard>`, replace FieldLabel (lines 105-107) with `<FieldLabel>`, replace showSaved+toast with `useToast()`, replace isMobile with `useIsMobile()`
- **StaffHub**: replace isMobile with `useIsMobile()`
- **LyricsSession**: replace IBtn/PillBtn with shared `IconButton`/`PillButton`, update import
- Delete all inline function definitions after swap

**Dependencies:**
- Depends on: TASK-003, TASK-004, TASK-005, TASK-006, TASK-008, TASK-009, TASK-010, TASK-011, TASK-012
- Depended by: TASK-047 through TASK-058 (feature decomposition — cleaner files to start with)

**Supabase Impact:** None

**Validation Steps:**
- Each modal opens/closes identically in MemberManager, ContentManager, FinanceManager
- Toasts display and auto-dismiss with correct timing (2500ms for MemberManager/FinanceManager, 2200ms for ContentManager)
- StatCards render identical values
- Search inputs function identically
- isMobile behavior identical (check at 768px)
- All icon buttons render with same dimensions
- No console errors

**Risk:** MEDIUM — ModalShell extraction has 3 consumers with slightly different padding/spacing. Must verify each renders identically.

**Rollback Plan:**
Revert each file's imports individually. Keep shared components — they become dead code until re-adopted.

---

# PHASE 2: Service Layer

## TASK-015: Create services/supabase.js (copy from lib)

**Type:** Service

**Goal:**
Copy `src/lib/supabase.js` to `src/services/supabase.js` as the foundation for the service layer. Both files coexist during migration.

**Files Affected:**
- COPY: `src/lib/supabase.js` → `src/services/supabase.js`

**Changes:**
- Copy `src/lib/supabase.js` verbatim to `src/services/supabase.js`
- Same exports: `export const supabase = createClient(supabaseUrl, supabaseAnonKey)`
- Same env var validation: throws Error if missing

**Dependencies:**
- Depends on: TASK-007 (directories exist)
- Depended by: TASK-016 through TASK-046 (all service files import supabase from here)

**Supabase Impact:** All tables — this is the client singleton.

**Validation Steps:**
- `src/services/supabase.js` exists and is identical to `src/lib/supabase.js`
- Import works without errors
- All existing imports from `../lib/supabase` continue to work (both files active)

**Risk:** LOW

**Rollback Plan:**
Delete `src/services/supabase.js`. Everything still works from `lib/`.

---

## TASK-016: Create services/members.js — query functions

**Type:** Service

**Goal:**
Extract all member-related Supabase queries from MemberManager and StaffHub into `src/services/members.js`.

**Files Affected:**
- CREATE: `src/services/members.js`

**Changes:**
Create the following exported functions, each wrapping an exact Supabase query chain:

- `getMembers()` → `supabase.from('members').select('*').order('name', { ascending: true })`
- `getMembersFinance()` → `supabase.from('members').select('id, name, contact_number, address').order('name')`
- `getMembersCount()` → `supabase.from('members').select('*', { count: 'exact', head: true })`
- `createMember(payload)` → `supabase.from('members').insert(payload)`
- `bulkInsertMembers(payloads)` → `supabase.from('members').insert(payloads)`
- `updateMember(id, payload)` → `supabase.from('members').update(payload).eq('id', id)`
- `deleteMember(id)` → `supabase.from('members').delete().eq('id', id)`

All functions return `{ data, error }` — identical raw Supabase response. No transformation.

**Dependencies:**
- Depends on: TASK-015 (services/supabase.js)
- Depended by: TASK-022, TASK-025, TASK-028 (MemberManager, FinanceManager, StaffHub service swap)

**Supabase Impact:** `members` table — read, insert, update, delete.

**Validation Steps:**
- Each function can be called and returns the same response shape as direct supabase call
- Query filters and ordering match exact source queries from .execution.md
- No console errors

**Risk:** MEDIUM — each function signature must match the caller's expectations exactly.

**Rollback Plan:**
Delete the file. Pages still use direct supabase calls.

---

## TASK-017: Create services/events.js — query functions

**Type:** Service

**Goal:**
Extract all event-related Supabase queries from ContentManager, LandingPage, and StaffHub into `src/services/events.js`.

**Files Affected:**
- CREATE: `src/services/events.js`

**Changes:**
Create the following exported functions:

- `getUpcomingEvents()` → `supabase.from('events').select('*').gte('date', today).order('date').order('time')`
- `getUpcomingEventsCount()` → `supabase.from('events').select('*', { count: 'exact', head: true }).gte('date', today)`
- `createEvent(payload)` → `supabase.from('events').insert(payload)`
- `updateEvent(id, payload)` → `supabase.from('events').update(payload).eq('id', id)`
- `deleteEvent(id)` → `supabase.from('events').delete().eq('id', id)`

All functions return `{ data, error }`. No transformation.

**Dependencies:**
- Depends on: TASK-015
- Depended by: TASK-024, TASK-026, TASK-028 (ContentManager, LandingPage, StaffHub service swap)

**Supabase Impact:** `events` table — read, insert, update, delete.

**Validation Steps:**
- `getUpcomingEvents` applies gte filter with `new Date().toISOString().split('T')[0]` for today
- `getUpcomingEventsCount` uses `head: true`
- Query chains match .execution.md exactly

**Risk:** MEDIUM

**Rollback Plan:**
Delete the file.

---

## TASK-018: Create services/content.js — query functions

**Type:** Service

**Goal:**
Extract all verse_library, carousel_items, and roster queries from ContentManager and LandingPage into `src/services/content.js`.

**Files Affected:**
- CREATE: `src/services/content.js`

**Changes:**
Create the following exported functions:

**Verse library:**
- `getAllVerses()` → `supabase.from('verse_library').select('*').order('created_at', { ascending: false })`
- `getActiveVerse()` → `supabase.from('verse_library').select('*').eq('is_active', true).maybeSingle()`
- `getVerseByReference(ref)` → `supabase.from('verse_library').select('id').eq('reference', ref).maybeSingle()`
- `createVerse(payload)` → `supabase.from('verse_library').insert(payload)`
- `deleteVerse(id)` → `supabase.from('verse_library').delete().eq('id', id)`
- `deactivateAllVerses()` → `supabase.from('verse_library').update({ is_active: false }).eq('is_active', true)`
- `activateVerse(id)` → `supabase.from('verse_library').update({ is_active: true }).eq('id', id)`
- `insertVerseFromCSV(row)` → individual `supabase.from('verse_library').insert({ ... })` (per row, NOT batch)

**Roster:**
- `getRosters(limit = null)` → `supabase.from('roster').select('*').order('week_start')().limit(limit)` (limit optional)
- `updateRoster(id, payload)` → `supabase.from('roster').update(payload).eq('id', id)`

**Carousel items:**
- `getAllCarouselItems()` → `supabase.from('carousel_items').select('*').order('display_order').order('created_at')`
- `getActiveCarouselItems()` → `supabase.from('carousel_items').select('*').eq('is_active', true).order('display_order').order('created_at')`
- `createCarouselItem(payload)` → `supabase.from('carousel_items').insert(payload)`
- `updateCarouselItem(id, payload)` → `supabase.from('carousel_items').update(payload).eq('id', id)`
- `deleteCarouselItem(id)` → `supabase.from('carousel_items').delete().eq('id', id)`
- `toggleCarouselItem(id, isActive)` → `supabase.from('carousel_items').update({ is_active: !isActive, updated_at: new Date() }).eq('id', id)`
- `updateCarouselItemOrder(id, displayOrder)` → `supabase.from('carousel_items').update({ display_order: displayOrder }).eq('id', id)`

All functions return `{ data, error }`. No transformation.

**Dependencies:**
- Depends on: TASK-015
- Depended by: TASK-024, TASK-026 (ContentManager, LandingPage service swap)

**Supabase Impact:** `verse_library`, `roster`, `carousel_items` tables.

**Validation Steps:**
- Each function builds the exact query chain from source
- Optional params (like limit) don't alter default behavior
- No console errors

**Risk:** MEDIUM

**Rollback Plan:**
Delete the file.

---

## TASK-019: Create services/finance.js — query functions

**Type:** Service

**Goal:**
Extract all membership_renewal queries from FinanceManager and StaffHub into `src/services/finance.js`.

**Files Affected:**
- CREATE: `src/services/finance.js`

**Changes:**
Create the following exported functions:

- `getRenewals()` → `supabase.from('membership_renewals').select('*').order('payment_date', { ascending: false })`
- `getRenewalsCount(year)` → `supabase.from('membership_renewals').select('*', { count: 'exact', head: true }).eq('renewal_year', year)`
- `recordRenewal(payload)` → `supabase.from('membership_renewals').insert(payload)`

All functions return `{ data, error }`. No transformation.

**Dependencies:**
- Depends on: TASK-015
- Depended by: TASK-025, TASK-028 (FinanceManager, StaffHub service swap)

**Supabase Impact:** `membership_renewals` table.

**Validation Steps:**
- `getRenewalsCount(year)` applies correct year filter
- `recordRenewal` returns 23505 error for duplicates (same behavior as direct call)
- No console errors

**Risk:** MEDIUM

**Rollback Plan:**
Delete the file.

---

## TASK-020: Create services/lyrics.js — query functions

**Type:** Service

**Goal:**
Extract all song/session queries from LyricsSession into `src/services/lyrics.js`.

**Files Affected:**
- CREATE: `src/services/lyrics.js`

**Changes:**
Create the following exported functions:

- `getSongs()` → `supabase.from('songs').select('id, title, lyrics').order('title')`
- `getActiveSessions()` → `supabase.from('public_sessions').select('session_code, expires_at, created_at').or(\`expires_at.is.null,expires_at.gt.${now}\`).order('created_at', { ascending: false }).limit(10)`
- `findSessionByCode(code)` → `supabase.from('public_sessions').select('*').eq('session_code', code).maybeSingle()`
- `getSessionSongs(sessionId)` → `supabase.from('session_songs').select('song_id').eq('session_id', sessionId)`
- `getSongsByIds(ids)` → `supabase.from('songs').select('id, title, lyrics').in('id', ids)`
- `checkCodeAvailability(code)` → `supabase.from('public_sessions').select('session_code').eq('session_code', code).maybeSingle()`
- `createSession(payload)` → `supabase.from('public_sessions').insert(payload).select().single()`
- `createSessionSongs(payloads)` → `supabase.from('session_songs').insert(payloads)` (batch)

All functions return `{ data, error }`. No transformation.

**Dependencies:**
- Depends on: TASK-015
- Depended by: TASK-056 (LyricsSession service swap)

**Supabase Impact:** `songs`, `public_sessions`, `session_songs` tables.

**Validation Steps:**
- `getActiveSessions` uses correct OR filter with expires_at
- `findSessionByCode` returns maybeSingle
- `createSessionSongs` accepts array of objects
- No console errors

**Risk:** MEDIUM

**Rollback Plan:**
Delete the file.

---

## TASK-021: Create services/auth.js — auth helper functions

**Type:** Service

**Goal:**
Extract Supabase auth operations from AuthContext into a service module. AuthContext retains its state management; the service provides the raw SDK calls.

**Files Affected:**
- CREATE: `src/services/auth.js`
- NO CHANGE to AuthContext yet

**Changes:**
Create the following exported functions:

- `getSession()` → `supabase.auth.getSession()`
- `onAuthStateChange(callback)` → `supabase.auth.onAuthStateChange(callback)`
- `signInWithPassword(email, password)` → `supabase.auth.signInWithPassword({ email, password })`
- `signOut()` → `supabase.auth.signOut()`

These are thin wrappers that map to exact Supabase Auth SDK calls used in AuthContext.jsx and Login.jsx.

**Dependencies:**
- Depends on: TASK-015
- Depended by: TASK-027 (AuthContext migration), TASK-030 (Login.jsx migration)

**Supabase Impact:** Supabase Auth — getSession, signIn, signOut.

**Validation Steps:**
- Each function maps to exact SDK call
- Return value shape matches direct SDK call
- No console errors

**Risk:** HIGH — auth operations affect login/logout. Any regression locks users out.

**Rollback Plan:**
Delete the file. AuthContext and Login continue using supabase directly.

---

## TASK-022: Swap MemberManager to use services/members.js

**Type:** Service

**Goal:**
Replace all direct `supabase.from('members')` calls in MemberManager with imports from `services/members.js`.

**Files Affected:**
- MODIFY: `src/pages/MemberManager.jsx`
  - `supabase.from('members').select('*').order('name')` at line 336 → `getMembers()`
  - `supabase.from('members').update(payload).eq('id', id)` at line 390 → `updateMember(id, payload)`
  - `supabase.from('members').insert(payload)` at line 393 → `createMember(payload)`
  - `supabase.from('members').delete().eq('id', id)` at line 412 → `deleteMember(id)`
  - `supabase.from('members').insert(importPreview)` at line 488 → `bulkInsertMembers(importPreview)`
  - `import { supabase } from '../lib/supabase'` at line 5 → REMOVE (if no other supabase usage remains)

**Changes:**
- Replace all 5 supabase query calls with equivalent service function calls
- Remove `supabase` import if no longer needed
- Add `import { getMembers, createMember, updateMember, deleteMember, bulkInsertMembers } from '../services/members'`
- All behaviors preserved identically — same data, same error handling

**Dependencies:**
- Depends on: TASK-016 (services/members.js exists)
- Depended by: nothing

**Supabase Impact:** `members` table — all operations now routed through service layer.

**Validation Steps:**
- MemberManager loads member list
- Add/Create member works
- Edit/Update member works
- Delete member works
- Bulk import works
- Network tab shows same query patterns

**Risk:** MEDIUM — import path changes, function signature must match call sites exactly.

**Rollback Plan:**
Revert imports to direct supabase calls. Delete import of services/members.

---

## TASK-023: Swap ContentManager service queries (verse_library)

**Type:** Service

**Goal:**
Replace all direct `supabase.from('verse_library')` calls in ContentManager with imports from `services/content.js`.

**Files Affected:**
- MODIFY: `src/pages/ContentManager.jsx`
  - `supabase.from('verse_library').select('*').order(...)` at line 128 → `getAllVerses()`
  - `supabase.from('verse_library').select('*').eq('is_active', true).maybeSingle()` at line 129 → `getActiveVerse()`
  - `supabase.from('verse_library').select('id').eq('reference', ref).maybeSingle()` at line 147 → `getVerseByReference(ref)`
  - `supabase.from('verse_library').insert({...})` at line 149 → `createVerse(payload)`
  - `supabase.from('verse_library').update({ is_active: false }).eq('is_active', true)` at line 155 → `deactivateAllVerses()`
  - `supabase.from('verse_library').update({ is_active: true }).eq('id', id)` at line 156 → `activateVerse(id)`
  - `supabase.from('verse_library').delete().eq('id', id)` at line 162 → `deleteVerse(id)`
  - CSV loop insert at line 186 → `createVerse(rowPayload)`

**Changes:**
- Replace all 8 verse_library query calls with service function calls
- Add imports from `../services/content`
- Remove `supabase` import only if no other direct supabase calls remain in file (events, roster, carousel still use direct calls — they will be migrated in TASK-024)

**Dependencies:**
- Depends on: TASK-018 (services/content.js exists)
- Depended by: nothing

**Supabase Impact:** `verse_library` table — read, insert, update, delete.

**Validation Steps:**
- Verse library loads
- Add verse works
- Delete verse works
- Activate verse works (two-step update)
- CSV import works
- No console errors

**Risk:** MEDIUM

**Rollback Plan:**
Revert imports for verse functions.

---

## TASK-024: Swap ContentManager service queries (events, roster, carousel)

**Type:** Service

**Goal:**
Replace remaining direct Supabase calls in ContentManager (events, roster, carousel_items) with service functions from `services/events.js` and `services/content.js`.

**Files Affected:**
- MODIFY: `src/pages/ContentManager.jsx`
  - `supabase.from('events').select('*').gte(...)` at line 130 → `getUpcomingEvents()`
  - `supabase.from('events').update(p).eq('id', id)` / `.insert(p)` at line 200 → `updateEvent(id, p)` / `createEvent(p)`
  - `supabase.from('events').delete().eq('id', id)` at line 204 → `deleteEvent(id)`
  - `supabase.from('roster').select('*').order('week_start').limit(4)` at line 131 → `getRosters(4)`
  - `supabase.from('roster').update(...).eq('id', id)` at line 210 → `updateRoster(id, payload)`
  - `supabase.from('carousel_items').select('*').order(...)` at line 132 → `getAllCarouselItems()`
  - `supabase.from('carousel_items').update(p).eq('id', id)` / `.insert(p)` at line 220 → `updateCarouselItem(id, p)` / `createCarouselItem(p)`
  - `supabase.from('carousel_items').delete().eq('id', id)` at line 224 → `deleteCarouselItem(id)`
  - `supabase.from('carousel_items').update({ is_active: !cur })` at line 225 → `toggleCarouselItem(id, cur)`
  - `supabase.from('carousel_items').update({ display_order }).eq('id', id)` x2 at line 232 → `updateCarouselItemOrder(id, order)`
  - Remove `import { supabase } from '../lib/supabase'` entirely

**Changes:**
- Replace all event/roster/carousel direct calls with service functions
- Add imports from `../services/events` and `../services/content`
- Remove `supabase` import from `../lib/supabase`
- All query behavior preserved identically

**Dependencies:**
- Depends on: TASK-017 (services/events.js), TASK-018 (services/content.js), TASK-023 (verse service swap completed)
- Depended by: nothing

**Supabase Impact:** `events`, `roster`, `carousel_items` tables.

**Validation Steps:**
- All 4 tabs in ContentManager load data
- Event add/edit/delete works
- Roster edit works
- Announcement add/edit/delete/toggle/reorder works
- Calendar modal loads preview
- No console errors

**Risk:** MEDIUM — largest single service swap task (10+ query replacements).

**Rollback Plan:**
Revert imports to direct supabase calls.

---

## TASK-025: Swap FinanceManager to use services/members.js and services/finance.js

**Type:** Service

**Goal:**
Replace all direct Supabase calls in FinanceManager with imports from `services/members.js` and `services/finance.js`.

**Files Affected:**
- MODIFY: `src/pages/FinanceManager.jsx`
  - `supabase.from('members').select('id, name, contact_number, address').order('name')` at line 175 → `getMembersFinance()`
  - `supabase.from('membership_renewals').select('*').order('payment_date', { ascending: false })` at line 176 → `getRenewals()`
  - `supabase.from('membership_renewals').insert({...})` at lines 228-234 → `recordRenewal(payload)`
  - Remove `import { supabase } from '../lib/supabase'`

**Changes:**
- Replace all 3 direct supabase calls with service functions
- Add imports from `../services/members` and `../services/finance`
- Remove supabase import
- All behavior preserved identically

**Dependencies:**
- Depends on: TASK-016, TASK-019
- Depended by: nothing

**Supabase Impact:** `members`, `membership_renewals` tables.

**Validation Steps:**
- FinanceManager loads members and renewals
- Record payment works (insert + refetch)
- 23505 error handling preserved
- Export CSV works
- Donut chart renders correctly
- No console errors

**Risk:** MEDIUM

**Rollback Plan:**
Revert imports.

---

## TASK-026: Swap LandingPage to use services/content.js and services/events.js

**Type:** Service

**Goal:**
Replace all direct Supabase calls in LandingPage with imports from `services/content.js` and `services/events.js`.

**Files Affected:**
- MODIFY: `src/pages/LandingPage.jsx`
  - `supabase.from('verse_library').select('*').eq('is_active', true).maybeSingle()` at lines 110-115 → `getActiveVerse()`
  - `supabase.from('events').select('*').gte('date', today).order('date').order('time')` at lines 118-126 → `getUpcomingEvents()`
  - `supabase.from('carousel_items').select('*').eq('is_active', true).order('display_order').order('created_at')` at lines 129-138 → `getActiveCarouselItems()`
  - `supabase.from('roster').select('*').order('week_start')` at lines 140-144 → `getRosters()`
  - Remove `import { supabase } from '../lib/supabase'` at line 11

**Changes:**
- Replace all 4 direct supabase calls with service functions
- The rosterMap processing (lines 146-181) stays in LandingPage — it's client-side data transformation, not a query
- Add imports from `../services/content` and `../services/events`
- Remove supabase import

**Dependencies:**
- Depends on: TASK-017, TASK-018
- Depended by: nothing

**Supabase Impact:** `verse_library`, `events`, `carousel_items`, `roster` tables.

**Validation Steps:**
- LandingPage loads on mount
- Active verse displays in about section
- Events modal shows events
- Carousel items render in "What's On"
- Roster modal shows roster data
- No console errors

**Risk:** MEDIUM

**Rollback Plan:**
Revert imports.

---

## TASK-027: Update AuthContext to use services/auth.js

**Type:** Service

**Goal:**
Replace direct `supabase.auth.*` calls in AuthContext with imports from `services/auth.js`.

**Files Affected:**
- MODIFY: `src/contexts/AuthContext.jsx`
  - `supabase.auth.getSession()` at line 12 → `getSession()`
  - `supabase.auth.onAuthStateChange(callback)` at line 18 → `onAuthStateChange(callback)`
  - `supabase.auth.signOut()` at line 27 → `signOut()`
  - `import { supabase } from '../lib/supabase'` at line 2 → REMOVE
  - ADD `import { getSession, onAuthStateChange, signOut } from '../services/auth'`

**Changes:**
- Replace 3 SDK calls with service function calls
- Remove supabase import
- All auth behavior preserved identically
- Auth state management (useState + useEffect) unchanged

**Dependencies:**
- Depends on: TASK-021 (services/auth.js exists)
- Depended by: nothing

**Supabase Impact:** Auth operations — getSession, onAuthStateChange, signOut.

**Validation Steps:**
- AuthProvider mounts, getSession fires on mount
- Session resolves → user set
- Auth state change listener fires correctly
- signOut works
- No console errors

**Risk:** HIGH — auth must not break. Any regression locks all staff pages.

**Rollback Plan:**
Revert AuthContext to direct supabase.auth calls. This must be tested immediately.

---

## TASK-028: Swap StaffHub to use service functions

**Type:** Service

**Goal:**
Replace all direct Supabase calls in StaffHub with imports from `services/events.js`, `services/members.js`, and `services/finance.js`.

**Files Affected:**
- MODIFY: `src/pages/StaffHub.jsx`
  - `supabase.from('events').select('*', { count: 'exact', head: true }).gte('date', today)` at line 182 → `getUpcomingEventsCount()`
  - `supabase.from('members').select('*', { count: 'exact', head: true })` at line 183 → `getMembersCount()`
  - `supabase.from('membership_renewals').select('*', { count: 'exact', head: true }).eq('renewal_year', year)` at line 184 → `getRenewalsCount(year)`
  - Remove `import { supabase } from '../lib/supabase'` at line 9

**Changes:**
- Replace all 3 count queries with service function calls
- Add imports from event/members/finance services
- Remove supabase import

**Dependencies:**
- Depends on: TASK-016, TASK-017, TASK-019
- Depended by: nothing

**Supabase Impact:** `events`, `members`, `membership_renewals` tables (count queries only).

**Validation Steps:**
- StaffHub loads stats
- Upcoming events count displays
- Pending renewals count displays
- Sign out works
- No console errors

**Risk:** LOW — count queries only, no data modification.

**Rollback Plan:**
Revert imports.

---

## TASK-029: Swap LyricsSession to use services/lyrics.js

**Type:** Service

**Goal:**
Replace all direct Supabase calls in LyricsSession with imports from `services/lyrics.js`.

**Files Affected:**
- MODIFY: `src/pages/LyricsSession.jsx`
  - `supabase.from('songs').select('id, title, lyrics').order('title')` at line 95 → `getSongs()`
  - `supabase.from('public_sessions').select(...).or(...).order(...).limit(10)` at lines 108-113 → `getActiveSessions()`
  - `supabase.from('public_sessions').select('*').eq('session_code', code).maybeSingle()` at lines 126-127 → `findSessionByCode(code)`
  - `supabase.from('session_songs').select('song_id').eq('session_id', id)` at line 131 → `getSessionSongs(sessionId)`
  - `supabase.from('songs').select('id, title, lyrics').in('id', [...])` at line 134 → `getSongsByIds(ids)`
  - `supabase.from('public_sessions').select('session_code').eq('session_code', code).maybeSingle()` inside `generateCode()` at line 52 → `checkCodeAvailability(code)`
  - `supabase.from('public_sessions').insert({...}).select().single()` at line 153-154 → `createSession(payload)`
  - `supabase.from('session_songs').insert([...])` at line 160 → `createSessionSongs(payloads)`
  - Remove `import { supabase } from '../lib/supabase'` at line 4

**Changes:**
- Replace all 8 direct supabase calls with service function calls
- Add imports from `../services/lyrics`
- Remove supabase import
- All behavior preserved identically

**Dependencies:**
- Depends on: TASK-020 (services/lyrics.js exists)
- Depended by: nothing

**Supabase Impact:** `songs`, `public_sessions`, `session_songs` tables.

**Validation Steps:**
- LyricsSession loads songs list
- Create session works (generateCode, insert session, insert songs, navigate)
- Join session works (lookup by code, load songs, display lyrics)
- Active sessions list loads
- Copy link works
- No console errors

**Risk:** MEDIUM

**Rollback Plan:**
Revert imports.

---

## TASK-030: Update Login.jsx to use services/auth.js

**Type:** Service

**Goal:**
Replace `supabase.auth.signInWithPassword()` in Login.jsx with import from `services/auth.js`.

**Files Affected:**
- MODIFY: `src/pages/Login.jsx`
  - `supabase.auth.signInWithPassword({ email, password })` at line 19 → `signInWithPassword(email, password)`
  - Remove `import { supabase } from '../lib/supabase'` at line 5
  - ADD `import { signInWithPassword } from '../services/auth'`

**Changes:**
- Replace single supabase.auth call with service function
- Remove supabase import
- Login flow preserved identically

**Dependencies:**
- Depends on: TASK-021 (services/auth.js exists)
- Depended by: nothing

**Supabase Impact:** Auth — signInWithPassword.

**Validation Steps:**
- Login form renders
- Sign in with valid credentials navigates to /staff
- Sign in with invalid credentials shows error
- No console errors

**Risk:** HIGH — auth regression locks staff access.

**Rollback Plan:**
Revert Login.jsx import.

---

## TASK-031: Remove src/lib/supabase.js after all consumers migrated

**Type:** Cleanup

**Goal:**
Remove the old `src/lib/supabase.js` file after confirming all consumers have been migrated to `src/services/supabase.js`.

**Files Affected:**
- DELETE: `src/lib/supabase.js`

**Changes:**
- Only proceed if no file in `src/` still imports from `../lib/supabase` or `../lib/supabase.js`
- Verify all imports point to `../services/supabase` or domain services
- Delete `src/lib/supabase.js`

**Dependencies:**
- Depends on: TASK-022 through TASK-030 (all pages migrated to service layer)
- Depended by: nothing

**Supabase Impact:** None — services/supabase.js is identical.

**Validation Steps:**
- Grep for all imports of `../lib/supabase` — zero results
- App still runs with services/supabase.js
- No 404s in network tab

**Risk:** LOW — additive removal after verification.

**Rollback Plan:**
Restore `src/lib/supabase.js` from git.

---

# PHASE 3: Feature Decomposition

## TASK-032: Move AdminTools to features/admin-tools

**Type:** Feature

**Goal:**
Move `src/pages/AdminTools.jsx` to `src/features/admin-tools/AdminTools.jsx` with no changes.

**Files Affected:**
- MOVE: `src/pages/AdminTools.jsx` → `src/features/admin-tools/AdminTools.jsx`
- MODIFY: `src/App.jsx` (import path)

**Changes:**
- Copy file to destination
- Update import in App.jsx from `'./pages/AdminTools'` to `'./features/admin-tools/AdminTools'`
- Delete original file

**Dependencies:**
- Depends on: TASK-007 (directory exists)
- Depended by: TASK-058 (App.jsx route consolidation)

**Supabase Impact:** None — placeholder page.

**Validation Steps:**
- /staff/admin route renders "Coming Soon"
- No console errors

**Risk:** LOW

**Rollback Plan:**
Revert App.jsx import, restore file to pages/.

---

## TASK-033: Move Login to features/auth

**Type:** Feature

**Goal:**
Move `src/pages/Login.jsx` to `src/features/auth/Login.jsx` with no changes.

**Files Affected:**
- MOVE: `src/pages/Login.jsx` → `src/features/auth/Login.jsx`
- MODIFY: `src/App.jsx` (import path)

**Changes:**
- Copy file to destination
- Update import in App.jsx from `'./pages/Login'` to `'./features/auth/Login'`
- Delete original file

**Dependencies:**
- Depends on: TASK-007 (directory exists), TASK-030 (Login already uses auth service)
- Depended by: TASK-058

**Supabase Impact:** None — already using services/auth.

**Validation Steps:**
- /login route renders login form
- Sign in works
- No console errors

**Risk:** LOW

**Rollback Plan:**
Revert App.jsx import, restore file.

---

## TASK-034: Extract ModuleCard from StaffHub

**Type:** Feature

**Goal:**
Extract the `MobileCard` function (StaffHub.jsx lines 87-123) and `DesktopCard` function (lines 126-162) into `src/features/dashboard/components/ModuleCard.jsx`.

**Files Affected:**
- CREATE: `src/features/dashboard/components/ModuleCard.jsx`
- MODIFY: `src/pages/StaffHub.jsx` (replace inline components)

**Changes:**
- Create `ModuleCard.jsx` exporting `MobileCard` and `DesktopCard` as named exports
- Both accept props: `mod`, `stats`, `onClick`
- Same responsive rendering: DesktopCard for desktop, MobileCard for mobile
- Same hover state management internally
- Import both in StaffHub, replace inline definitions
- Delete inline functions from StaffHub

**Dependencies:**
- Depends on: TASK-007 (directory exists)
- Depended by: TASK-036 (StaffHub feature move)

**Supabase Impact:** None

**Validation Steps:**
- StaffHub renders same module cards
- Hover effects identical
- Badges render for content and finance modules
- Inactive modules show "Soon" badge
- No console errors

**Risk:** LOW

**Rollback Plan:**
Revert inline functions.

---

## TASK-035: Move StaffHub to features/dashboard

**Type:** Feature

**Goal:**
Move `src/pages/StaffHub.jsx` to `src/features/dashboard/StaffHub.jsx` with updated imports.

**Files Affected:**
- MOVE: `src/pages/StaffHub.jsx` → `src/features/dashboard/StaffHub.jsx`
- MODIFY: `src/App.jsx` (import path)

**Changes:**
- Copy file to destination
- Update import path for `ModuleCard` from relative path to `./components/ModuleCard`
- Update import in App.jsx from `'./pages/StaffHub'` to `'./features/dashboard/StaffHub'`
- Delete original file

**Dependencies:**
- Depends on: TASK-034 (ModuleCard exists), TASK-028 (StaffHub uses service layer)
- Depended by: TASK-058

**Supabase Impact:** None

**Validation Steps:**
- /staff route renders dashboard
- Module cards navigate correctly
- Sign out works
- Stats load
- No console errors

**Risk:** LOW

**Rollback Plan:**
Revert App.jsx import, restore file.

---

## TASK-036: Extract DonutChart from FinanceManager

**Type:** Feature

**Goal:**
Extract the `DonutRing` SVG component (FinanceManager.jsx lines 60-78) into `src/features/finance/components/DonutChart.jsx`.

**Files Affected:**
- CREATE: `src/features/finance/components/DonutChart.jsx`
- MODIFY: `src/pages/FinanceManager.jsx` (replace inline)

**Changes:**
- Create `DonutChart.jsx` exporting `DonutRing` (default)
- Props: `pct` (number 0-100)
- Same SVG: 112×112 viewBox, circle with strokeDasharray/offset, percentage text, "paid" label
- Same styling, colors (C.accentDark for stroke, C.text for text, C.textMuted for label)
- Import in FinanceManager, replace inline DonutRing
- Delete inline DonutRing function from FinanceManager

**Dependencies:**
- Depends on: TASK-007 (directory exists)
- Depended by: TASK-039 (FinanceManager feature move)

**Supabase Impact:** None

**Validation Steps:**
- Donut chart renders with correct percentage
- SVG paths identical to inline version
- No console errors

**Risk:** LOW

**Rollback Plan:**
Revert inline function.

---

## TASK-037: Extract PaymentModal from FinanceManager

**Type:** Feature

**Goal:**
Extract the payment modal form from FinanceManager (lines 218-241, 300+ rendering) into `src/features/finance/components/PaymentModal.jsx`.

**Files Affected:**
- CREATE: `src/features/finance/components/PaymentModal.jsx`
- MODIFY: `src/pages/FinanceManager.jsx`

**Changes:**
- Create `PaymentModal.jsx` with props: `isOpen`, `onClose`, `member`, `selectedYear`, `paymentForm`, `onFormChange`, `onSubmit`
- Move JSX for the payment modal (amount input, date picker, method pills, receipt, notes) into this component
- Use shared `<Modal>` component for the shell
- Replace inline payment modal JSX in FinanceManager with `<PaymentModal>`
- All form state management stays in FinanceManager (orchestrator)

**Dependencies:**
- Depends on: TASK-003 (Modal component), TASK-007 (directory exists)
- Depended by: TASK-039

**Supabase Impact:** None

**Validation Steps:**
- Payment modal opens with correct member info
- Form fields work (amount, date, method, receipt, notes)
- Submit calls onSubmit handler
- Close calls onClose
- No console errors

**Risk:** LOW-MEDIUM — form state is in parent; ensure all fields map correctly.

**Rollback Plan:**
Revert to inline JSX.

---

## TASK-038: Extract FinanceManager stat components (MiniStat, StatCard inline)

**Type:** Feature

**Goal:**
The FinanceManager has inline `MiniStat` (lines 81-88) and uses its own `StatCard` (lines 91-102). `MiniStat` is unique to FinanceManager — extract to feature component. `StatCard` should already use shared component from TASK-005.

**Files Affected:**
- CREATE: `src/features/finance/components/MiniStat.jsx`
- MODIFY: `src/pages/FinanceManager.jsx`

**Changes:**
- Create `MiniStat.jsx` with props: `label`, `value`, `hi` (boolean, highlights with dark background)
- Styling matches source: `flexShrink:0`, `borderRadius:12px`, dark/light variants
- Replace inline MiniStat function in FinanceManager with import
- Delete inline MiniStat function

**Dependencies:**
- Depends on: TASK-007 (directory exists)
- Depended by: TASK-039

**Supabase Impact:** None

**Validation Steps:**
- Mobile stat strip renders same values
- Highlighted stats have dark background
- No console errors

**Risk:** LOW

**Rollback Plan:**
Revert to inline function.

---

## TASK-039: Move FinanceManager to features/finance

**Type:** Feature

**Goal:**
Move `src/pages/FinanceManager.jsx` to `src/features/finance/FinanceManager.jsx` with updated imports.

**Files Affected:**
- MOVE: `src/pages/FinanceManager.jsx` → `src/features/finance/FinanceManager.jsx`
- MODIFY: `src/App.jsx` (import path)

**Changes:**
- Copy file to destination
- Update import paths for DonutChart, PaymentModal, MiniStat to relative paths
- Update import in App.jsx from `'./pages/FinanceManager'` to `'./features/finance/FinanceManager'`
- Delete original file

**Dependencies:**
- Depends on: TASK-036, TASK-037, TASK-038 (sub-components exist), TASK-025 (uses service layer)
- Depended by: TASK-058

**Supabase Impact:** None

**Validation Steps:**
- /staff/finance route loads
- Donut chart renders
- Payment modal works
- Export CSV works
- Filter/search works
- No console errors

**Risk:** MEDIUM — moved file with multiple component imports. Verify all paths.

**Rollback Plan:**
Revert App.jsx import, restore file.

---

## TASK-040: Extract JoinSession from LyricsSession

**Type:** Feature

**Goal:**
Extract the "Join a Session" UI section (LyricsSession.jsx lines 212-249) into `src/features/lyrics/components/JoinSession.jsx`.

**Files Affected:**
- CREATE: `src/features/lyrics/components/JoinSession.jsx`
- MODIFY: `src/pages/LyricsSession.jsx`

**Changes:**
- Create `JoinSession.jsx` with props: `joinCode`, `onJoinCodeChange`, `onJoin`, `activeSessions`, `onSessionClick`, `sessionsLoading`
- JSX: join card with code input (4-digit, numeric only), Join button, active sessions list
- Styling matches source exactly
- Replace inline JSX in LyricsSession with `<JoinSession>`

**Dependencies:**
- Depends on: TASK-007 (directory exists)
- Depended by: TASK-043 (LyricsSession feature move)

**Supabase Impact:** None

**Validation Steps:**
- Join session UI renders
- Code input accepts only 4 digits
- Join button disabled until 4 digits entered
- Active sessions list shows
- No console errors

**Risk:** LOW

**Rollback Plan:**
Revert to inline JSX.

---

## TASK-041: Extract CreateSession from LyricsSession

**Type:** Feature

**Goal:**
Extract the "Create Session" modal (LyricsSession.jsx lines 142-165, render section) into `src/features/lyrics/components/CreateSession.jsx`.

**Files Affected:**
- CREATE: `src/features/lyrics/components/CreateSession.jsx`
- MODIFY: `src/pages/LyricsSession.jsx`

**Changes:**
- Create `CreateSession.jsx` with props: `showCreate`, `onClose`, `createCode`, `expiryHours`, `onExpiryChange`, `songs`, `selectedIds`, `onToggleSong`, `search`, `onSearchChange`, `onCreate`, `isCreating`, `error`
- JSX: session code display, expiry selector (1h/2h/4h/8h pills), song search, song checkbox list, create button
- Styling matches source exactly
- Replace inline create modal JSX in LyricsSession with `<CreateSession>`
- State management stays in parent (LyricsSession orchestrator)

**Dependencies:**
- Depends on: TASK-007 (directory exists), TASK-003 (Modal component)
- Depended by: TASK-043

**Supabase Impact:** None

**Validation Steps:**
- Create modal opens
- Song search filters list
- Song check/uncheck works
- Expiry selector works
- Create button disabled when no songs selected
- No console errors

**Risk:** LOW-MEDIUM — complex modal with search + checkboxes. Verify all state binds correctly.

**Rollback Plan:**
Revert to inline JSX.

---

## TASK-042: Extract SessionViewer from LyricsSession

**Type:** Feature

**Goal:**
Extract the lyrics reader/session viewer (LyricsSession.jsx lines 267-304+) into `src/features/lyrics/components/SessionViewer.jsx`.

**Files Affected:**
- CREATE: `src/features/lyrics/components/SessionViewer.jsx`
- MODIFY: `src/pages/LyricsSession.jsx`

**Changes:**
- Create `SessionViewer.jsx` with props: `sessionData`, `joinCode`, `activeSongIdx`, `totalSongs`, `currentSong`, `onPrev`, `onNext`, `onSongTabClick`, `onCopyLink`, `copied`, `onClose`
- JSX: session header (code, song count, copy/close buttons), song title bar, lyrics display (line-by-line with `\n` split, blank lines as `·`), song tab navigation, prev/next with dot indicators
- Replace inline lyrics reader JSX in LyricsSession with `<SessionViewer>`
- Navigation handlers pass through to parent

**Dependencies:**
- Depends on: TASK-007 (directory exists)
- Depended by: TASK-043

**Supabase Impact:** None

**Validation Steps:**
- Session loads and displays song title
- Lyrics render line-by-line
- Next/Prev navigation works
- Song tabs work
- Copy link works
- Close returns to join view
- No console errors

**Risk:** LOW-MEDIUM — verify song index sync between component and parent.

**Rollback Plan:**
Revert to inline JSX.

---

## TASK-043: Move LyricsSession to features/lyrics

**Type:** Feature

**Goal:**
Move `src/pages/LyricsSession.jsx` to `src/features/lyrics/LyricsSession.jsx` with updated imports.

**Files Affected:**
- MOVE: `src/pages/LyricsSession.jsx` → `src/features/lyrics/LyricsSession.jsx`
- MODIFY: `src/App.jsx` (import path)

**Changes:**
- Copy file to destination
- Update import paths for JoinSession, CreateSession, SessionViewer to relative paths
- Update import in App.jsx from `'./pages/LyricsSession'` to `'./features/lyrics/LyricsSession'`
- Delete original file

**Dependencies:**
- Depends on: TASK-040, TASK-041, TASK-042 (sub-components exist), TASK-029 (uses service layer)
- Depended by: TASK-058

**Supabase Impact:** None

**Validation Steps:**
- /lyrics route loads
- Create session works
- Join session works
- /lyrics/join/:code auto-joins
- Copy link works
- No console errors

**Risk:** MEDIUM

**Rollback Plan:**
Revert App.jsx import, restore file.

---

## TASK-044: Extract EventsTab from ContentManager

**Type:** Feature

**Goal:**
Extract the Events tab content from ContentManager (events list, add/edit/delete) into `src/features/content/components/EventsTab.jsx`.

**Files Affected:**
- CREATE: `src/features/content/components/EventsTab.jsx`
- MODIFY: `src/pages/ContentManager.jsx`

**Changes:**
- Create `EventsTab.jsx` with props: `events`, `onAdd`, `onEdit`, `onDelete`, `evtOpen`, `editEvt`, `evtForm`, `onFormChange`, `onSaveEvent`, `onCloseForm`
- Move events rendering JSX (event cards, add/edit modal, form fields) into this component
- Use shared `<Modal>` and `<PillButton>` and `<IconButton>` components
- Replace inline events section in ContentManager with `<EventsTab>`

**Dependencies:**
- Depends on: TASK-003 (Modal), TASK-009 (shared buttons), TASK-007 (directory exists)
- Depended by: TASK-046 (ContentManager feature move)

**Supabase Impact:** None

**Validation Steps:**
- Events tab lists upcoming events with date/title/time/location/PIC
- Add event form works
- Edit event pre-fills correctly
- Delete event with confirmation works
- Time conversion (12h/24h) preserved
- No console errors

**Risk:** MEDIUM — event form has time format conversion (to12/to24) that must be preserved exactly.

**Rollback Plan:**
Revert to inline JSX.

---

## TASK-045: Extract VerseTab from ContentManager

**Type:** Feature

**Goal:**
Extract the Weekly Verse tab content from ContentManager (verse library list, search, add/edit/delete, activate, CSV import/export) into `src/features/content/components/VerseTab.jsx`.

**Files Affected:**
- CREATE: `src/features/content/components/VerseTab.jsx`
- MODIFY: `src/pages/ContentManager.jsx`

**Changes:**
- Create `VerseTab.jsx` with props: `verseLib`, `activeVerse`, `verseSearch`, `onSearchChange`, `selectedVerse`, `onSelectVerse`, `showAddVerse`, `onToggleAdd`, `newVerse`, `onNewVerseChange`, `onSaveVerse`, `onActivateVerse`, `onDeleteVerse`, `onExportCSV`, `onImportCSV`, `selVerse`
- Move verse rendering JSX (library list panel, active verse preview, selected verse preview, add form, CSV buttons) into this component
- Replace inline verse section in ContentManager with `<VerseTab>`

**Dependencies:**
- Depends on: TASK-003 (Modal), TASK-009 (PillButton), TASK-007
- Depended by: TASK-046

**Supabase Impact:** None

**Validation Steps:**
- Verse tab lists all verses
- Search filters verse list
- Add verse form works
- Activate verse triggers two-step process
- Delete verse works with confirmation
- CSV export downloads file
- CSV import parses and inserts rows
- No console errors

**Risk:** MEDIUM — CSV import/export logic must be preserved in parent.

**Rollback Plan:**
Revert to inline JSX.

---

## TASK-046: Extract RosterTab and AnnouncementsTab from ContentManager

**Type:** Feature

**Goal:**
Extract the Roster and Announcements tab content from ContentManager into separate components.

**Files Affected:**
- CREATE: `src/features/content/components/RosterTab.jsx`
- CREATE: `src/features/content/components/AnnouncementsTab.jsx`
- MODIFY: `src/pages/ContentManager.jsx`

**Changes:**
- Create `RosterTab.jsx` with props: `roster`, `rstOpen`, `editRst`, `rstForm`, `onFormChange`, `onEditRoster`, `onSaveRoster`, `onCloseRoster`
  - JSX: week cards grid (leader/pianist/reader), edit button per card, edit modal
- Create `AnnouncementsTab.jsx` with props: `announces`, `onAdd`, `onEdit`, `onDelete`, `onToggle`, `onMoveUp`, `onMoveDown`, `annOpen`, `editAnn`, `annForm`, `onFormChange`, `onSaveAnnouncement`, `onCloseForm`
  - JSX: carousel items list with order controls, thumbnail, bilingual titles, toggle active, edit/delete
- Replace inline roster and announcement sections in ContentManager

**Dependencies:**
- Depends on: TASK-003 (Modal), TASK-009 (PillButton/IconButton), TASK-007
- Depended by: TASK-047 (ContentManager feature move)

**Supabase Impact:** None

**Validation Steps:**
- Roster tab shows week cards
- Edit roster works
- Announcements tab shows carousel items
- Toggle active works
- Reorder up/down works
- Add/edit announcement works
- Delete announcement works
- No console errors

**Risk:** MEDIUM

**Rollback Plan:**
Revert to inline JSX.

---

## TASK-047: Move ContentManager to features/content

**Type:** Feature

**Goal:**
Move `src/pages/ContentManager.jsx` to `src/features/content/ContentManager.jsx` with updated imports.

**Files Affected:**
- MOVE: `src/pages/ContentManager.jsx` → `src/features/content/ContentManager.jsx`
- MODIFY: `src/App.jsx` (import path)

**Changes:**
- Copy file to destination
- Update import paths for VerseTab, EventsTab, RosterTab, AnnouncementsTab to relative paths
- Remove now-unused imports (Label, Field, IBtn, PillBtn, ModalShell — now in shared components)
- Update import in App.jsx from `'./pages/ContentManager'` to `'./features/content/ContentManager'`
- Delete original file

**Dependencies:**
- Depends on: TASK-044, TASK-045, TASK-046 (all content sub-components exist), TASK-023, TASK-024 (uses service layer)
- Depended by: TASK-058

**Supabase Impact:** None

**Validation Steps:**
- /staff/content route loads
- All 4 tabs render and function
- Calendar modal opens
- All CRUD operations work
- No console errors

**Risk:** MEDIUM — largest feature move with 4+ sub-component imports.

**Rollback Plan:**
Revert App.jsx import, restore file.

---

## TASK-048: Extract EventsModal and RosterModal from LandingPage

**Type:** Feature

**Goal:**
Extract the Events modal (list + detail drill-down) and Roster modal (month/week selector) from LandingPage into `src/features/public-home/components/`.

**Files Affected:**
- CREATE: `src/features/public-home/components/EventsModal.jsx`
- CREATE: `src/features/public-home/components/RosterModal.jsx`
- MODIFY: `src/pages/LandingPage.jsx`

**Changes:**
- Create `EventsModal.jsx` with props: `isOpen`, `onClose`, `events`, `selectedEvent`, `onSelectEvent`, `locale`
  - Two views: list (event cards with date badge, title, time) and detail (selected event full info with back button)
  - Uses shared `<Modal>` component
  - Time formatting via `formatTimeForDisplay` and `formatEventDate` (copy these helpers or import)
- Create `RosterModal.jsx` with props: `isOpen`, `onClose`, `availableMonths`, `selectedMonth`, `onMonthChange`, `selectedWeek`, `onWeekChange`, `rosterForSelectedWeek`, `locale`, `t`
  - Month selector, week pills (1-4), roster card (leader/pianist/reader or "no roster" message)
- Replace inline modal JSX in LandingPage with `<EventsModal>` and `<RosterModal>`
- Move helper functions `formatTimeForDisplay` and `formatEventDate` into a shared `src/features/public-home/hooks/useEventFormat.js` or keep in modal file

**Dependencies:**
- Depends on: TASK-003 (Modal), TASK-007 (directory exists)
- Depended by: TASK-051 (LandingPage feature move)

**Supabase Impact:** None

**Validation Steps:**
- Events modal opens with event list
- Click event card shows detail view
- Back returns to list
- Close closes modal
- Roster modal opens with month selector
- Changing month updates roster
- Week pills work (1-4)
- No console errors

**Risk:** LOW-MEDIUM — modals with drill-down state.

**Rollback Plan:**
Revert to inline JSX.

---

## TASK-049: Extract HeroSection from LandingPage

**Type:** Feature

**Goal:**
Extract the hero section (video background, overlay text, "What's On" Splide carousel) from LandingPage into `src/features/public-home/components/HeroSection.jsx`.

**Files Affected:**
- CREATE: `src/features/public-home/components/HeroSection.jsx`
- MODIFY: `src/pages/LandingPage.jsx`

**Changes:**
- Create `HeroSection.jsx` with props: `videoError`, `onVideoError`, `carouselItems`, `whatsOnSplideRef`, `locale`, `t`, `onOpenEvents`, `onOpenRoster`
- Receive splide initialization as a prop or use a ref forwarded from parent — the Splide `useEffect` stays in LandingPage (it orchestrates initialization)
- The hero JSX includes: video background (with fallback image on error), overlay gradient, welcome text + CTA, "What's On" carousel wrapper div with `id="whats-on-splide"`, arrow buttons (`.whats-on-arrow-prev`, `.whats-on-arrow-next`), events/roster buttons
- Replace inline hero JSX section in LandingPage with `<HeroSection>`

**Dependencies:**
- Depends on: TASK-007 (directory exists)
- Depended by: TASK-051

**Supabase Impact:** None

**Validation Steps:**
- Hero section renders with video/gradient
- Fallback image shows when video errors
- Carousel placeholder div renders (Splide init still in parent)
- Events/Roster buttons fire parent handlers
- No console errors

**Risk:** LOW

**Rollback Plan:**
Revert to inline JSX.

---

## TASK-050: Extract NavBar, AboutSection, HowWeDoChurch, Footer from LandingPage

**Type:** Feature

**Goal:**
Extract remaining major sections from LandingPage into feature sub-components.

**Files Affected:**
- CREATE: `src/features/public-home/components/NavBar.jsx`
- CREATE: `src/features/public-home/components/AboutSection.jsx`
- CREATE: `src/features/public-home/components/HowWeDoChurch.jsx`
- CREATE: `src/features/public-home/components/Footer.jsx`
- MODIFY: `src/pages/LandingPage.jsx`

**Changes:**
- Create `NavBar.jsx` with props: `scrolled`, `locale`, `toggleLocale`, `menuOpen`, `onToggleMenu`, `navLinks`, `getNavText`, `onNavClick`
  - JSX: top nav bar with logo, BM/EN toggle, Menu/Close button, fullscreen overlay menu with staggered links
- Create `AboutSection.jsx` with props: `verse`, `t`, `locale`, `onOpenEvents`, `onOpenRoster`
  - JSX: about header image, tagline, about text, active verse display, action buttons
- Create `HowWeDoChurch.jsx` with props: `howWeSplideRef`
  - JSX: "How We Do Church" carousel wrapper with `id="how-we-splide"`, 4 placeholder cards, arrow buttons (`.how-we-arrow-prev`, `.how-we-arrow-next`)
  - Splide useEffect stays in LandingPage
- Create `Footer.jsx` with props: `t`
  - JSX: logo, church name, address, watch online link, phone, tagline
- Replace all inline JSX sections in LandingPage

**Dependencies:**
- Depends on: TASK-007 (directory exists)
- Depended by: TASK-051

**Supabase Impact:** None

**Validation Steps:**
- Navbar renders with correct scroll state
- Menu opens/closes with overlay
- Toggle locale navigates BM/EN
- About section shows verse
- How We Do church carousel wrapper renders
- Footer renders
- No console errors

**Risk:** LOW

**Rollback Plan:**
Revert to inline JSX.

---

## TASK-051: Move LandingPage to features/public-home

**Type:** Feature

**Goal:**
Replace `src/pages/LandingPage.jsx` with `src/features/public-home/index.jsx` as the entry point for the public home feature.

**Files Affected:**
- COPY: `src/pages/LandingPage.jsx` → `src/features/public-home/index.jsx`
- MODIFY: `src/App.jsx` (import path)
- DELETE: `src/pages/LandingPage.jsx` (after verification)

**Changes:**
- Copy file to destination as `index.jsx`
- Update import paths for NavBar, HeroSection, AboutSection, HowWeDoChurch, EventsModal, RosterModal, Footer to relative paths
- Remove now-unused imports (formatTimeForDisplay, formatEventDate — now in modal files)
- Update import in App.jsx from `'./pages/LandingPage'` to `'./features/public-home'`
- Delete original file
- The orchestrator retains: all 14 useState calls, 5 useEffects, scroll handler, body scroll lock, Splide initialization effects, loadContent with 4 service calls, rosterMap processing

**Dependencies:**
- Depends on: TASK-048, TASK-049, TASK-050 (all sub-components exist), TASK-026 (uses service layer)
- Depended by: TASK-058

**Supabase Impact:** None

**Validation Steps:**
- / route renders full homepage
- All sections render (hero, about, how-we-do, marquee, giving, footer)
- Events modal works
- Roster modal works
- Menu opens/closes
- Locale toggle works
- No console errors

**Risk:** MEDIUM — 1174 LOC file moved with 8+ sub-component imports.

**Rollback Plan:**
Revert App.jsx import, restore file to pages/.

---

## TASK-052: Extract MemberProfile modal from MemberManager

**Type:** Feature

**Goal:**
Extract the `MemberProfileModal` (MemberManager.jsx lines 155-245) into `src/features/members/components/MemberProfile.jsx`.

**Files Affected:**
- CREATE: `src/features/members/components/MemberProfile.jsx`
- MODIFY: `src/pages/MemberManager.jsx`

**Changes:**
- Create `MemberProfile.jsx` with props: `member`, `onClose`, `isMobile`
- Extract all JSX: overlay, panel, header with avatar, info rows, church life section
- Uses helper functions `calcAge`, `toDisplay` — import from parent or accept as props
- Mobile responsive via isMobile prop
- Replace inline MemberProfileModal in MemberManager with `<MemberProfile>`

**Dependencies:**
- Depends on: TASK-007 (directory exists)
- Depended by: TASK-056 (MemberManager feature move)

**Supabase Impact:** None

**Validation Steps:**
- Click member name opens profile modal
- Profile shows avatar, name, DOB/age, address, contact, gender/status, dates
- Deceased members show cross icon and death date
- Close button and overlay click work
- No console errors

**Risk:** LOW

**Rollback Plan:**
Revert to inline function.

---

## TASK-053: Extract MemberForm from MemberManager

**Type:** Feature

**Goal:**
Extract the add/edit member form modal (MemberManager.jsx lines 372-397, form rendering JSX) into `src/features/members/components/MemberForm.jsx`.

**Files Affected:**
- CREATE: `src/features/members/components/MemberForm.jsx`
- MODIFY: `src/pages/MemberManager.jsx`

**Changes:**
- Create `MemberForm.jsx` with props: `isOpen`, `onClose`, `formData`, `onInputChange`, `onSubmit`, `editingId`, `isMobile`
- JSX: form fields (name, sex radio, address, contact, DOB with DateInput, registered since, baptism date, marital status select, is_deceased checkbox, date_of_death)
- Uses shared `<Modal>`, `<FieldLabel>`, `<PillButton>` components
- Uses DateInput component (moved inline from MemberManager lines 64-73)
- Replace inline form modal in MemberManager with `<MemberForm>`

**Date handling**: Form uses `formData` object with raw fields (name, sex, address, etc.). The `handleSubmit` in MemberManager builds the payload — this stays in the parent. The DateInput component handles DD/MM/YYYY ↔ YYYY-MM-DD conversion internally.

**Dependencies:**
- Depends on: TASK-003 (Modal), TASK-008 (FieldLabel), TASK-009 (PillButton), TASK-007
- Depended by: TASK-056

**Supabase Impact:** None

**Validation Steps:**
- Add member form opens with empty fields
- Edit member form opens pre-filled
- Sex radio buttons work
- Marital status dropdown works
- Date fields accept DD/MM/YYYY and convert to YYYY-MM-DD
- Deceased checkbox shows/hides date_of_death field
- Submit calls onSubmit
- Close calls onClose
- No console errors

**Risk:** MEDIUM — date format conversion must be preserved exactly (DD/MM/YYYY display, YYYY-MM-DD storage).

**Rollback Plan:**
Revert to inline form JSX.

---

## TASK-054: Extract BulkImportModal from MemberManager

**Type:** Feature

**Goal:**
Extract the bulk CSV import modal (MemberManager.jsx lines 432-493) into `src/features/members/components/BulkImportModal.jsx`.

**Files Affected:**
- CREATE: `src/features/members/components/BulkImportModal.jsx`
- MODIFY: `src/pages/MemberManager.jsx`

**Changes:**
- Create `BulkImportModal.jsx` with props: `isOpen`, `onClose`, `onImport`, `isMobile`, `fileInputRef`
- JSX: template download section, file upload section, preview table, import errors list, Import button
- Keep the character-by-character CSV parser (lines 456-461) in the parent — this is complex logic that should not be duplicated. The modal handles the UI; the parent handles file reading and parsing.
- OR: move parsing into the modal as a callback:
  - Modal renders file input
  - On file select, reads with FileReader, calls `onImport(parsedData)` with parsed result
  - Parent handles validation and Supabase insert
- Replace inline bulk import modal in MemberManager with `<BulkImportModal>`

**Dependencies:**
- Depends on: TASK-003 (Modal), TASK-009 (PillButton), TASK-007
- Depended by: TASK-056

**Supabase Impact:** None

**Validation Steps:**
- Bulk import modal opens
- Template download creates CSV with headers + example rows
- File upload reads CSV
- Invalid CSV shows errors
- Valid CSV shows preview
- Import button triggers parent insert
- No console errors

**Risk:** MEDIUM — CSV parser is character-by-character with quote handling. Must not be duplicated or altered.

**Rollback Plan:**
Revert to inline.

---

## TASK-055: Extract MemberTable and MemberCard from MemberManager

**Type:** Feature

**Goal:**
Extract the desktop data table and mobile card list from MemberManager into separate feature components.

**Files Affected:**
- CREATE: `src/features/members/components/MemberTable.jsx`
- CREATE: `src/features/members/components/MemberCard.jsx`
- MODIFY: `src/pages/MemberManager.jsx`

**Changes:**
- Create `MemberTable.jsx` with props: `filteredMembers`, `stats`, `onEdit`, `onDelete`, `onSelectMember`, `searchTerm`, `onSearchChange`, `ageFilter`, `onAgeFilterChange`, `onExportCSV`, `onOpenAdd`, `onOpenBulkImport`, `isMobile`
  - Desktop: filter bar, data table with columns (Name + initials, Sex, Address, Status, Actions)
  - Mobile: not rendered (uses MemberCard instead)
- Create `MemberCard.jsx` with props: `member`, `onEdit`, `onDelete`, `onSelect`
  - Mobile card: avatar circle, name (click→profile), address + age, badges (age + marital status), edit/delete buttons
- Replace inline rendering in MemberManager with `<MemberTable>` and `<MemberCard>`

**Dependencies:**
- Depends on: TASK-002 (Badge), TASK-006 (SearchInput), TASK-005 (StatCard), TASK-007
- Depended by: TASK-056

**Supabase Impact:** None

**Validation Steps:**
- Desktop: table renders with all columns, sort by name, age/sex badges, edit/delete buttons
- Mobile: card list renders
- Search filters list
- Age filter pills work
- Deceased rows show dimmed
- Export CSV works
- No console errors

**Risk:** MEDIUM — complex rendering with multiple badges, conditional states.

**Rollback Plan:**
Revert to inline rendering.

---

## TASK-056: Move MemberManager to features/members

**Type:** Feature

**Goal:**
Move `src/pages/MemberManager.jsx` to `src/features/members/MemberManager.jsx` with updated imports.

**Files Affected:**
- MOVE: `src/pages/MemberManager.jsx` → `src/features/members/MemberManager.jsx`
- MODIFY: `src/App.jsx` (import path)

**Changes:**
- Copy file to destination
- Update import paths for MemberProfile, MemberForm, BulkImportModal, MemberTable, MemberCard to relative paths
- Update import paths from `'../services/...'` to `'../../services/...'`
- Remove now-unused inline component imports (Badge, ModalShell etc. — now in shared)
- Update import in App.jsx from `'./pages/MemberManager'` to `'./features/members/MemberManager'`
- Delete original file

**Dependencies:**
- Depends on: TASK-052, TASK-053, TASK-054, TASK-055 (all sub-components exist), TASK-022 (uses service layer)
- Depended by: TASK-058

**Supabase Impact:** None

**Validation Steps:**
- /staff/members route loads
- All CRUD operations work
- Search and age filter work
- Profile modal opens
- Bulk import works
- Export CSV works
- No console errors

**Risk:** MEDIUM — largest feature move with 5+ sub-component imports.

**Rollback Plan:**
Revert App.jsx import, restore file.

---

## TASK-057: Clean up App.jsx imports pointing to features

**Type:** Cleanup

**Goal:**
Update all App.jsx imports to point to feature directories instead of pages/. Remove the old pages/ directory.

**Files Affected:**
- MODIFY: `src/App.jsx` (all import paths)

**Changes:**
- Update all 8 imports: `'./pages/X'` → `'./features/X'` or `'./features/X/X'`
- After all imports updated and verified, delete `src/pages/` directory

**Import mapping:**
- `'./pages/LandingPage'` → `'./features/public-home'`
- `'./pages/Login'` → `'./features/auth/Login'`
- `'./pages/StaffHub'` → `'./features/dashboard/StaffHub'`
- `'./pages/MemberManager'` → `'./features/members/MemberManager'`
- `'./pages/ContentManager'` → `'./features/content/ContentManager'`
- `'./pages/FinanceManager'` → `'./features/finance/FinanceManager'`
- `'./pages/AdminTools'` → `'./features/admin-tools/AdminTools'`
- `'./pages/LyricsSession'` → `'./features/lyrics/LyricsSession'`

**Dependencies:**
- Depends on: all feature move tasks (TASK-032 through TASK-056)
- Depended by: nothing

**Supabase Impact:** None

**Validation Steps:**
- All 8 routes render correctly
- No 404s on any route
- No import errors in console

**Risk:** MEDIUM — verify every single route after import update.

**Rollback Plan:**
Revert App.jsx imports, restore pages/ directory.

---

## TASK-058: Delete empty pages/ directory

**Type:** Cleanup

**Goal:**
Remove `src/pages/` after confirming all files have been migrated.

**Files Affected:**
- DELETE: `src/pages/` directory (all files already moved)

**Changes:**
- Verify all files in `src/pages/` have been moved
- Delete directory

**Dependencies:**
- Depends on: TASK-057 (App.jsx no longer imports from pages/)
- Depended by: nothing

**Supabase Impact:** None

**Validation Steps:**
- `ls src/pages/` returns "No such file or directory"
- App still runs

**Risk:** LOW

**Rollback Plan:**
Recreate directory (files are in features/).

---

# PHASE 4: Layout Extraction

## TASK-059: Create StaffLayout component

**Type:** Layout

**Goal:**
Create a shared `StaffLayout` component that wraps staff pages with header, sidebar/back button, and sign-out button.

**Files Affected:**
- CREATE: `src/components/layout/StaffLayout.jsx`
- NO CHANGE to consuming files in this task

**Changes:**
- Create `StaffLayout.jsx` exporting a default `StaffLayout` component
- Props: `title`, `subtitle`, `children`, `onBack` (navigate to '/staff'), `onSignOut`
- Uses `useIsMobile()` hook internally
- Mobile layout:
  - Header: back button (calls onBack), title + subtitle, sign out button
  - Children rendered below header
- Desktop layout:
  - Sticky header with back button, title, subtitle, sign out button
  - Content wrapper with children
- Sign out button uses `useAuth().signOut` pattern:
  ```
  const { signOut } = useAuth()
  const handleLogout = async () => { await signOut(); navigate('/login') }
  ```
- Styling matches the source pattern from all 4 staff pages:
  - Background: `#FAF8F5`, surface: `#FFFFFF`, border: `#EAE1D4`
  - Font: Lora for titles, DM Sans for body
  - Header: sticky, surface background, bottom border, 60px height
- Google Fonts `<link>` tag should be rendered once in this layout instead of in each page

**Dependencies:**
- Depends on: TASK-011 (useIsMobile), TASK-007 (directories exist)
- Depended by: TASK-060 (wrap staff pages)

**Supabase Impact:** None

**Validation Steps:**
- Component renders header with back button
- Title and subtitle display
- Sign out button renders (wire in next task)
- Children render in content area
- Mobile layout differs from desktop
- No console errors

**Risk:** LOW

**Rollback Plan:**
Delete file.

---

## TASK-060: Wrap staff pages with StaffLayout

**Type:** Layout

**Goal:**
Wrap StaffHub, MemberManager, ContentManager, and FinanceManager with `<StaffLayout>`, removing duplicated header/back/logout JSX.

**Files Affected:**
- MODIFY: `src/features/dashboard/StaffHub.jsx`
- MODIFY: `src/features/members/MemberManager.jsx`
- MODIFY: `src/features/content/ContentManager.jsx`
- MODIFY: `src/features/finance/FinanceManager.jsx`

**Changes:**
- In each staff page:
  - Remove inline header JSX (back button, title, sign out button)
  - Remove inline Google Fonts `<link>` tag
  - Remove `handleLogout` function and `signOut` usage from the page (handled by StaffLayout)
  - Remove `useAuth().signOut` call (now in StaffLayout)
  - Wrap the main content return with `<StaffLayout title="..." subtitle="...">`
- **StaffHub** does NOT use back button (it IS the dashboard). StaffLayout must support `hideBack` prop.
- **StaffHub** keeps its own handleLogout (currently inline in header) — StaffLayout provides `onSignOut` prop
- Page provides: `title`, `subtitle` (optional), `hideBack` (optional), `onSignOut` (required for all except StaffHub which can use default)

**Dependencies:**
- Depends on: TASK-059 (StaffLayout exists)
- Depended by: nothing

**Supabase Impact:** None

**Validation Steps:**
- All staff pages render correctly wrapped in layout
- Back button navigates to /staff from sub-pages
- Sign out works from all pages
- Google Fonts still load (from layout)
- Desktop vs mobile layout correct
- No console errors

**Risk:** MEDIUM — layout extraction touches 4 pages, sign-out flow must work on all.

**Rollback Plan:**
Revert each page individually. StaffLayout component remains for future use.

---

## TASK-061: Move Google Fonts link from individual pages to layout/index.html

**Type:** Layout

**Goal:**
Remove duplicated Google Fonts `<link>` tags from all pages and inject once in a single location.

**Files Affected:**
- MODIFY: `src/features/public-home/index.jsx` (remove Google Fonts link)
- MODIFY: `src/features/dashboard/StaffHub.jsx` (remove link)
- MODIFY: `src/features/members/MemberManager.jsx` (remove link)
- MODIFY: `src/features/content/ContentManager.jsx` (remove link)
- MODIFY: `src/features/finance/FinanceManager.jsx` (remove link)
- MODIFY: `src/features/lyrics/LyricsSession.jsx` (remove link)
- MODIFY: `src/components/layout/StaffLayout.jsx` (already has link from TASK-059)
- OPTIONAL: `index.html` (add single link tag for Google Fonts)

**Changes:**
- Remove Google Fonts `<link>` elements from all page-level render returns
- StaffLayout already injects the link for staff pages
- Add a single `<link>` to `index.html` `<head>` for the two font families (Lora + DM Sans)
- Or: inject in a single layout component that wraps the entire app

**Dependencies:**
- Depends on: TASK-060 (StaffLayout exists with link)
- Depended by: nothing

**Supabase Impact:** None

**Validation Steps:**
- Pages still render with correct fonts (Lora, DM Sans)
- No duplicate `<link>` tags in DOM
- No flash of unstyled text (fonts load from one location)
- No console errors

**Risk:** LOW — visual change only if a page's specific font subset differs.

**Rollback Plan:**
Restore `<link>` tags in each page.

---

# PHASE 5: Cleanup

## TASK-062: Remove unused inline component definitions across all files

**Type:** Cleanup

**Goal:**
After all extractions, identify and remove any inline component definitions that are no longer used within their files.

**Files Affected:**
- ALL feature files and remaining page files

**Changes:**
- Check each file for now-dead inline component definitions (e.g., old ModalShell, Badge, StatCard, FieldLabel, IBtn, PillBtn functions that were replaced by shared imports)
- Remove dead code
- Remove unused imports (especially `lucide-react` icons that were used only in removed sections)

**Dependencies:**
- Depends on: all extraction tasks complete
- Depended by: nothing

**Supabase Impact:** None

**Validation Steps:**
- App still runs
- No console errors
- No unused variable warnings

**Risk:** LOW

**Rollback Plan:**
Restore from git.

---

## TASK-063: Remove src/pages directory

**Type:** Cleanup

**Goal:**
Delete `src/pages/` after confirming all files have been migrated to `src/features/`.

**Files Affected:**
- DELETE: `src/pages/` (directory + all remaining files)

**Changes:**
- List files in `src/pages/` — if any remain, move them manually
- Only delete when directory is empty

**Dependencies:**
- Depends on: TASK-057 (App.jsx imports updated)
- Depended by: nothing

**Supabase Impact:** None

**Validation Steps:**
- `src/pages/` no longer exists
- All 8 routes still work

**Risk:** LOW

**Rollback Plan:**
`git checkout src/pages/`

---

## TASK-064: Remove src/lib directory

**Type:** Cleanup

**Goal:**
Delete `src/lib/` after verifying `src/lib/supabase.js` has been removed and no remaining imports reference `../lib/`.

**Files Affected:**
- DELETE: `src/lib/` (directory)

**Changes:**
- Verify no files remain in `src/lib/`
- Verify no imports reference `../lib/` anywhere in `src/`
- Delete directory

**Dependencies:**
- Depends on: TASK-031 (lib/supabase.js removed)
- Depended by: nothing

**Supabase Impact:** None

**Validation Steps:**
- Grep for `../lib/` returns 0 results
- App still runs

**Risk:** LOW

**Rollback Plan:**
`git checkout src/lib/`

---

# PHASE 6: Post-Migration Optimization (OPTIONAL)

## TASK-065: Add error boundary wrapper (OPTIONAL)

**Type:** Cleanup

**Goal:**
Add a React error boundary component to prevent uncaught render errors from crashing the entire component tree (addresses .audit.md R7).

**Files Affected:**
- CREATE: `src/components/ErrorBoundary.jsx`
- MODIFY: `src/App.jsx` (wrap routes)

**Changes:**
- Create a class-based `ErrorBoundary` component with `componentDidCatch` and `getDerivedStateFromError`
- Display a fallback UI ("Something went wrong" + reload button)
- Wrap `<AppRoutes>` with `<ErrorBoundary>` in App.jsx

**Dependencies:**
- Depends on: all structural migration complete
- Depended by: nothing

**Supabase Impact:** None

**Validation Steps:**
- Normal rendering unaffected
- Simulated error shows fallback UI (not white screen)
- Reload button recovers app

**Risk:** LOW — additive, no behavioral change to existing code.

**Rollback Plan:**
Remove ErrorBoundary import from App.jsx. Delete file.

---

## TASK-066: Normalize ContentManager toast timeout (OPTIONAL)

**Type:** Cleanup

**Goal:**
ContentManager uses 2200ms toast timeout while all other pages use 2500ms. Normalize to 2500ms (or keep as-is — documented discrepancy).

**Files Affected:**
- MODIFY: `src/features/content/ContentManager.jsx` (if normalizing)

**Changes:**
- Change `setTimeout(() => setToast(null), 2200)` to `setTimeout(() => setToast(null), 2500)` in ContentManager
- Or: keep the discrepancy documented if intentional

**Dependencies:**
- Depends on: feature migration complete
- Depended by: nothing

**Supabase Impact:** None

**Validation Steps:**
- Toasts in ContentManager dismiss after 2500ms (same as other pages)

**Risk:** LOW

**Rollback Plan:**
Revert to 2200ms.

---

## TASK-067: Add env var validation UI (OPTIONAL)

**Type:** Cleanup

**Goal:**
Prevent the app from crashing at module import time when env vars are missing (addresses .audit.md R8).

**Files Affected:**
- MODIFY: `src/services/supabase.js`
- OR: `src/main.jsx`

**Changes:**
- Move env var validation from module-level throw to a function that can fail gracefully
- OR: Add a check in main.jsx that renders a "Configuration Error" UI instead of the app

**Dependencies:**
- Depends on: all structural migration complete
- Depended by: nothing

**Supabase Impact:** None

**Validation Steps:**
- Without env vars: app shows error UI (not white screen)
- With env vars: app initializes normally

**Risk:** LOW — additive change.

**Rollback Plan:**
Revert validation change.

---

## TASK-068: Document known architectural issues (OPTIONAL)

**Type:** Cleanup

**Goal:**
Document the remaining architectural issues that cannot be addressed in this migration (race conditions, cross-page staleness, O(n²) computations).

**Files Affected:**
- UPDATE: `.audit.md` (mark resolved issues, note remaining)
- OR: CREATE `.known-issues.md`

**Changes:**
- List remaining issues:
  1. Login race condition (AuthContext vs navigate) — requires auth flow redesign
  2. Cross-page data staleness (ContentManager → LandingPage) — requires realtime subscriptions or shared state
  3. O(n²) hasPaidForYear computation — requires aggregated Supabase query or memoization refactor
  4. Non-transactional verse activation — requires database function or RPC
  5. Unbounded data loading (no pagination) — requires query limits
  6. No navigation guards — requires useBlocker
  7. Token expiry force-logout — requires graceful re-auth flow

**Dependencies:**
- Depends on: all migration complete
- Depended by: nothing

**Supabase Impact:** None

**Validation Steps:**
- Document is accurate reflection of remaining issues

**Risk:** LOW

**Rollback Plan:**
Delete file.

---

# SUMMARY

| Category | Count | Risk Profile |
|---|---|---|
| UI extraction | 14 | LOW |
| Hooks | 4 | LOW |
| Service layer | 32 | 17 LOW + 13 MEDIUM + 2 HIGH |
| Feature decomposition | 12 | 2 LOW + 10 MEDIUM |
| Layout | 3 | 2 LOW + 1 MEDIUM |
| Cleanup | 3 | LOW |
| **Total** | **68** | **29 LOW / 36 MEDIUM / 3 HIGH** |

**HIGH risk tasks**: TASK-021 (auth service), TASK-027 (AuthContext migration), TASK-030 (Login.jsx service swap) — all auth-related, guarded by immediate verification.

**Execution flow**:
```
Phase 1 (TASK-001-014, parallel-safe)
  ↓
Phase 2 (TASK-015-031, sequential per domain — each service + consumer pair)
  ↓
Phase 3 (TASK-032-058, sequential — smallest features first)
  ↓
Phase 4 (TASK-059-061, after features stabilized)
  ↓
Phase 5 (TASK-062-064, after everything migrated)
  ↓
Phase 6 (TASK-065-068, OPTIONAL — any order)
```
